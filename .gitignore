node_modules/
.claude/
.mcp.json
coverage/
junit.xml

# Dados locais e backups — não versionar (podem conter dados reais)
data/local-data.json
backups/*.json
backups/*.ndjson
backups/*.csv
backups/migration/
siga-images.tar

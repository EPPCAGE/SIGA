# ================================================================
# Pipeline CI — Validação de PRs (gesproc-CI)
# Dispara em commits/PRs para main, dev e feature/*
# ================================================================

trigger:
  branches:
    include:
      - main
      - dev
      - feature/*

pr:
  branches:
    include:
      - main
      - dev
      - feature/*

pool:
  name: "sefaz-self-hosted"

variables:
  NODE_VERSION: '20.x'

steps:
  - checkout: self
    clean: true
    fetchDepth: 0

#######################################
  ### GHAS - Init Codeql

  - task: AdvancedSecurity-Codeql-Init@1
    displayName: "🛡️ GHAS - Init"
    inputs:
      languages: "javascript"
      enableAutomaticCodeQLInstall: true

#######################################
  # ── SonarCloud 
  - task: SonarCloudPrepare@4
    displayName: "☁️ Sonar — preparar análise"
    inputs:
      SonarCloud: "sonarcloud-gesproc"
      organization: "sefaz-rs"
      scannerMode: "CLI"
      configMode: "file"
      configFile: "sonar-project.properties"
      extraProperties: |
        sonar.links.ci=$(System.TeamFoundationCollectionUri)$(System.TeamProject)/_build/results?buildId=$(Build.BuildId)

  - task: NodeTool@0
    displayName: "📦 Node.js $(NODE_VERSION)"
    inputs:
      versionSpec: "$(NODE_VERSION)"

  - script: npm ci
    displayName: "📥 npm ci"

  - script: npm run test
    displayName: "✅ Testes unitários"
    continueOnError: false

  - script: npm run test:coverage
    displayName: "📊 Cobertura de código"
    continueOnError: false

  - task: PublishTestResults@2
    displayName: "📋 Publicar resultados"
    condition: succeededOrFailed()
    inputs:
      testResultsFormat: JUnit
      testResultsFiles: "**/junit.xml"
      testRunTitle: "CI — $(Build.SourceBranchName)"
      failTaskOnFailedTests: true

  - task: PublishCodeCoverageResults@2
    displayName: "📈 Publicar cobertura"
    condition: succeededOrFailed()
    inputs:
      codeCoverageTool: Cobertura
      summaryFileLocation: "$(System.DefaultWorkingDirectory)/coverage/cobertura-coverage.xml"
      reportDirectory: "$(System.DefaultWorkingDirectory)/coverage"

  #######################################
  ### Run AdvancedSecurity Codeql Analyze

  - task: AdvancedSecurity-Dependency-Scanning@1
    displayName: "🛡️ GHAS - Scanning"

  - task: AdvancedSecurity-Codeql-Analyze@1
    displayName: "🛡️GHAS - Analyze"

  - task: AdvancedSecurity-Publish@1
    displayName: "🛡️ GHAS - Publish"

  - task: AdvancedSecurity-Gating-Local@1
    displayName: "🛡️ GHAS - Gating"
    condition: and(succeeded(), startsWith(variables['Build.SourceBranch'], 'refs/pull/'))
    inputs:
      alertSeverityCriteria: 'critical, high, warning, medium, error, low, note'
      alertTypeCriteria: 'dependency, code, secret'
      blockOnNewFindings: true
    continueOnError: true
 
  #######################################
  ### Run Sonar Analyze and Publish

  - task: SonarCloudAnalyze@4
    displayName: "🔎 Sonar Cloud - Analyze"

  - task: SonarCloudPublish@4
    displayName: "🔎 Sonar Cloud - Publish"